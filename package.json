{
  "name": "cesium-monorepo",
  "private": true,
  "engines": {
    "node": ">=12.2.0"
  },
  "version": "0.0.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "format": "prettier --write .",
    "lint": "eslint packages/*/{src,types}/**",
    "docs": "vitepress dev docs",
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "vite": "^2.7.13",
    "prettier": "^2.5.1",
    "eslint": "^8.8.0",
    "typescript": "^4.5.5"
  }
}
